package com.company;

import mocha.std.List;

class Entrypoint {
	void main() {
		List<Integer> list = List<Integer>();
		for (int i = 0; i < 10; i = i + 1) {
			list.add(i + 1);
		}

		System.out.println(list.toString());
	}
}